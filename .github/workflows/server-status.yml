name: GitHub Network Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Show public IP
        run: |
          echo "GitHub Runner public IP:"
          curl -4 https://api.ipify.org
          echo
